---
title: "Mushaf App"
subtitle: "The web application like other Muslim apps, provides Al-Quran include vocal audio, daily prayers, etc."
date: "2023-11-11"
image: "/assets_porto/mushaf_app/home.svg"
thumbnail: '/assets_porto/mushaf_app/mushaf.png'
theme: '#00AD7C'
stacks: ['TypeScript', 'React', 'Vite', 'PWA', 'MUI', 'ReactQuery', 'ReactRouter']
projectType: 'Website'
repository: 'https://github.com/ZnovandaP/mushaf-app'
livePreview: 'https://mushaf-app.vercel.app/'
---

## Introduction

This is personal website to showing my projects and blogs

## Features

### 📝 Blog

I love to share blog about programming on Dev.to and i integrated it here using dev.to api

### 📚 Roadmap

I share the roadmap how to become Software Engineer base on my learning path. I designing it by [roadmap.sh](https://roadmap.sh/) and [WPU](https://www.youtube.com/@sandhikagalihWPU). Let's see this awesome feature [here](https://www.codebayu.com/roadmap?tribe=frontend-developer)

### 🏵 Programming Tips

I share a bit tips about programming such as best practice, clean code, and fundamental programming. You can see this feature [here](https://www.codebayu.com/learn)

### 🎨 Portfolio

##Zidane
Hallo my name is zidane novanda pura

To showcasing my awesome projects. You can see this feature [here](https://www.codebayu.com/projects)

## How to Start this project

### 1. Clone using git

```
'use client';

import * as React from 'react';
import { LuClipboardCopy, LuCheckCircle } from 'react-icons/lu';
import { useCopyToClipboard } from 'usehooks-ts';
import Tooltip from '../Tooltip';

type ButtonCopyProps = {
  code: string
};


export default function ButtonCopy({ code }: ButtonCopyProps) {
  const [isCoppied, setIsCoppied] = React.useState(false);
  const timeoutRef = React.useRef<ReturnType<typeof setTimeout> | null>(null);
  // eslint-disable-next-line @typescript-eslint/no-unused-vars
  const [value, copy] = useCopyToClipboard();

  const handleCopyClipboard = () => {
    copy(code);
    setIsCoppied(true);
    timeoutRef.current = setTimeout(() => {
      setIsCoppied(false);
    }, 2000);
  };

  return (
    <Tooltip title="copy">
      <button
        className="center text-xl p-2 rounded-full ring-1 ring-stone-500/60 hover:scale-110 active:scale-100 transition duration-300"
        aria-label="Button Copy clipboard"
        type="button"
        onClick={handleCopyClipboard}
      >
        { !isCoppied ? (<LuClipboardCopy />) : (<LuCheckCircle className="text-green-500" />)}
      </button>
    </Tooltip>
  );
}

```

### 2. Install dependencies

```bash
yarn install
```

### 3. Run development server

```bash
yarn dev
```

Open http://localhost:3000 with your browser to see the result.
